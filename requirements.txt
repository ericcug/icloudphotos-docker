# Core dependencies: install directly from local submodule
-e libs/icloud_photos_downloader

# Other dependencies
python-telegram-bot>=20.0
PyYAML>=6.0

# Development/Testing (not included in Docker image)
pytest>=7.0
pytest-cov>=4.0
pytest-mock>=3.0
